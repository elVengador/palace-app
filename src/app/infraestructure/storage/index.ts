import { readLocalStorage, readSessionStorage, saveLocalStorage, saveSessionStorage } from "./storage";

export const storage = {
    saveLocalStorage,
    saveSessionStorage,
    readLocalStorage,
    readSessionStorage
}
